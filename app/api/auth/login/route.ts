import {NextResponse} from 'next/server';import {cookies} from 'next/headers';import {supabase} from '@/lib/supabase';import {SESSION_COOKIE} from '@/lib/admin'
export async function POST(req:Request){const b=await req.json().catch(()=>({}));const {data,error}=await supabase.rpc('admin_issue_session',{p_username:String(b.username||''),p_password:String(b.password||'')});if(error||!data)return NextResponse.json({error:'Invalid login.'},{status:401});const c=await cookies();c.set(SESSION_COOKIE,String(data),{httpOnly:true,secure:true,sameSite:'lax',path:'/',maxAge:43200});return NextResponse.json({ok:true})}