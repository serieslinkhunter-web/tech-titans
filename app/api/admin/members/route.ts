import {NextResponse} from "next/server";import {getAdminSupabase}from "@/lib/supabase";import {requireAdmin}from "@/lib/admin";const s=(v:any)=>String(v??"").trim();
export async function POST(req:Request){if(!(await requireAdmin()))return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();if(!s(b.name))return NextResponse.json({error:"Name is required."},{status:400});const payload={name:s(b.name),role:s(b.role)||null,branch:s(b.branch)||null,year:s(b.year)||null,image_url:s(b.image_url)||null};const db=getAdminSupabase();const {error}=b.id?await db.from("members").update(payload).eq("id",b.id):await db.from("members").insert(payload);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}
export async function DELETE(req:Request){if(!(await requireAdmin()))return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();const {error}=await getAdminSupabase().from("members").delete().eq("id",b.id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}