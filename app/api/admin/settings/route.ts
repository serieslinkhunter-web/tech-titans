import {NextResponse}from"next/server";import{getAdminSupabase}from"@/lib/supabase";import{requireAdmin}from"@/lib/admin";const s=(v:any)=>String(v??"").trim();
export async function POST(req:Request){if(!(await requireAdmin()))return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();const db=getAdminSupabase();const payload={club_email:s(b.club_email)||null,phone:s(b.phone)||null,instagram_url:s(b.instagram_url)||null,linkedin_url:s(b.linkedin_url)||null,website_url:s(b.website_url)||null,address:s(b.address)||null,updated_at:new Date().toISOString()};const {error}=await db.from("site_settings").update(payload).eq("id",true);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}